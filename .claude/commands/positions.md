---
description: Show Charon dry-run/live positions (headless)
allowed-tools:
  - Bash
---

```bash
node scripts/cmd.js positions
```

Report the captured output. If the user asks follow-ups, work from the text — don't re-run.
