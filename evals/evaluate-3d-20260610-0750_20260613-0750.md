---
window: 3d
generated_at: 2026-06-13 07:50 +0700
range_start: 2026-06-10 07:50 +0700
range_end: 2026-06-13 07:50 +0700
closed_trades: 13
win_rate_pct: 46.2
avg_pnl_pct: -0.06
total_pnl_sol: -0.0004
data_source: remote Postgres (pooled across instances; all rows from machine bc8b8596)
---

# Charon Evaluation — 3d

**Window:** 3d · **Range:** 2026-06-10 07:50 +0700 → 2026-06-13 07:50 +0700
**Generated:** 2026-06-13 07:50 +0700 · **Data source:** remote Postgres (pooled across instances)

## Headline

| Metric | Value |
|---|---|
| Closed trades | 13 |
| Win rate | 46.2% |
| Avg PnL | -0.06% |
| Median PnL | -1.07% |
| Total PnL | -0.0004 SOL |

## By strategy

| Strategy | Trades | Wins | Win rate | Avg PnL |
|---|---|---|---|---|
| degen | 13 | 6 | 46.2% | -0.06% |

## By exit reason

| Exit reason | Trades | Share | Avg PnL | Avg hold |
|---|---|---|---|---|
| TRAILING_TP | 7 | 53.8% | +13.70% | 15.3 min |
| SL | 6 | 46.2% | -16.11% | 2.3 min |

## By LLM confidence

LLM not used (rule-based strategy, `use_llm: false`) — confidence battery N/A. All 13 rows carry the orchestrator's `confidence: 100` sentinel (single 80+ bucket).

## Config drift

Stable — all 13 rows share: tp=30, sl=-10, trail=12, min_mcap=70000, max_mcap=1000000, min_swaps=100, min_vol=5000, use_llm=false. No pre-snapshot cohort in this window.

## Patterns

- **Trading halted 2.7 days ago and never resumed.** All 13 trades closed in one 5.5h session on Jun 10 (08:00–13:32 WIB). The window ended with 3 consecutive SLs (13:14, 13:22, 13:32); the last candidate was persisted at 13:31:08, and zero candidates or trades exist since. The SL-streak circuit breaker (f86a2b4, deployed Jun 10 06:03) triggered as designed at the third SL — but it has latched permanently (see Recommendations).
- **The breaker's first half worked.** It correctly paused new entries right as a selloff streak hit (13:14–13:32: −18.2%, −15.6%, −23.8%) — exactly the regime-day pattern it was built for (cf. Jun 07).
- **Trailing TP carried the session.** 7 trailing exits averaged +13.7% (best +30.6%, +25.8%), holding ~15 min, vs SLs averaging −16.1% in ~2 min. SL fills ranged −10.0% to −23.8% — the known rug/instant-dump tail, priced in. Net result before the halt: roughly breakeven (−0.06% avg).

## Recommendations

1. **Circuit breaker release path (code fix, top priority)** — Last candidate 13:31:08 Jun 10, third consecutive SL closed 13:32, zero candidates persisted for ~2.7 days since: `checkSlCooldown` in `src/pipeline/orchestrator.js` deadlocks. After the 1h cooldown expires, it re-queries `recentClosedExits(stratId, 3)` (`src/db/positions.js`), which has **no time bound** — it returns the same last-3 SL rows forever and re-arms a fresh cooldown on every candidate. The streak can never break because breaking it requires a new closed trade, which the breaker itself blocks. Fix: bound the streak query by recency (e.g. only count SLs with `closed_at_ms` inside the cooldown lookback) or clear the streak once a cooldown has fully elapsed. Interim manual unlatch on the production machine: raise the `sl_streak_cooldown_count` setting (e.g. to 999) — deleting `sl_cooldown_until_degen` alone won't help, the streak re-arms immediately.
2. **Verify production liveness after unlatching** — the latch fully explains the silence (skip happens before `upsertCandidate`, so no candidates land in Postgres), but a bot crash at ~13:32 Jun 10 would look identical from this side. Confirm the process is up when applying the fix.
3. **Hold all trading params** — tp=30 / sl=-10 / trail=12 / mcap band performed at breakeven over a small, halt-truncated sample. SL overshoot (avg −16.1% vs −10 configured) is the priced-in rug tail; trailing_percent=12 was settled by the Jun 02 sweep. No entry-filter or exit-param change is supported by 13 trades.

## Notes

- All rows from production machine `bc8b8596-719e-4fd0-b3be-f2de2b942d5f`; local SQLite belongs to a different machine (`01b682c5…`), so the production `sl_cooldown_until_degen` setting could not be read directly — the latch diagnosis rests on the code path plus the candidate-flow cutoff aligning to the minute with the third SL.
- Effective sample is one trading session (5.5h), not 3 days — treat the headline metrics as session stats, not a 3d regime read.
- `/learn 3d` not run (metrics-only evaluation; saves an LLM call).
- `pnl_sol` magnitudes are small (total −0.0004 SOL), consistent with small dry-run position sizing.
