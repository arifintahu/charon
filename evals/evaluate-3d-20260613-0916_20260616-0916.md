---
window: 3d
generated_at: 2026-06-16 09:16 +0700
range_start: 2026-06-13 09:16 +0700
range_end: 2026-06-16 09:16 +0700
closed_trades: 157
win_rate_pct: 57.3
avg_pnl_pct: 1.50
total_pnl_sol: 0.1179
data_source: remote Postgres (pooled across instances)
---

# Charon Evaluation — 3d

**Window:** 3d · **Range:** 2026-06-13 09:16 +0700 → 2026-06-16 09:16 +0700
**Generated:** 2026-06-16 09:16 +0700 · **Data source:** remote Postgres (pooled across instances)

## Headline

| Metric | Value |
|---|---|
| Closed trades | 157 |
| Win rate | 57.3% |
| Avg PnL | 1.50% |
| Median PnL | 2.53% |
| Total PnL | 0.1179 SOL |

## By strategy

| Strategy | Trades | Wins | Win rate | Avg PnL |
|---|---|---|---|---|
| degen | 157 | 90 | 57.3% | 1.50% |

## By exit reason

| Exit reason | Trades | Share | Avg PnL | Avg hold |
|---|---|---|---|---|
| TRAILING_TP | 92 | 58.6% | 15.97% | 17.2 min |
| SL | 65 | 41.4% | -18.98% | 5.1 min |

## By LLM confidence

LLM not used (rule-based strategy) — confidence battery N/A. `degen` runs `use_llm: false`, so the orchestrator writes a hardcoded `confidence: 100` sentinel for every rule-based decision that clears the gates. The bucket table collapses to a single `80+` row (n=157) at confidence 100 by design, not a screener bug.

## Config drift

Stable — all closed trades share: tp=30, sl=-10, trail=12, min_mcap=70000, max_mcap=1000000, trending_min_swaps=100, trending_min_volume_usd=5000, use_llm=false. Single cohort, apples-to-apples. No pre-snapshot / null-tp-sl cohort present in this window.

## Patterns

- Win rate 57.3% with median PnL (+2.53%) above the mean (+1.50%): the typical trade is a modest winner; the average is dragged down by a left-tail of deep losers.
- The book is two clean clusters: 92 TRAILING_TP exits at +15.97% (17.2 min hold) carry the strategy, and 65 SL exits at -18.98% (5.1 min hold) are the drag. The SL average of -18.98% is well past the -10% configured stop — fast, deep fills below the stop = the known `degen` rug / instant-dump tail.
- SL fills resolve in ~5 min vs ~17 min for winners: losers die fast and gap through the stop, winners are given room to trail. That asymmetry is the trailing-stop design working as intended on the winners' side.

## Recommendations

157 closed trades — recommendations apply.

1. **Hold the current degen config.** Net positive over the window (+1.50% avg, +0.1179 SOL, 57.3% win rate) on a stable cohort. tp=30 / sl=-10 / trail=12 / min_mcap=70k are all settled levers — no metric in this window argues for a change.
2. **Treat the SL tail as a priced-in cost, do not add an entry gate.** The 65 SL fills at -18.98% are the rug / instant-dump tail (deep fills below the -10% stop, 5.1 min hold). Entry filters, mcap raises, poll-cadence and stop-placement changes have all been investigated and are dead ends; the winners cluster (+15.97% × 92) still nets the book positive. Recommend hold over any entry filter.

## Notes

- `/learn` not run — no narrative lessons requested; metric battery only.
- `pg-sql.js` reads `process.env.POSTGRES_URL` and does not load `.env`; the value was injected from the project `.env` for this run. Emits a benign pg v3.0.0 SSL-mode deprecation warning to stderr — no impact on results.
- Config-drift query returned n=158 vs 157 closed elsewhere: one position carries a non-null `pnl_percent` edge case in the GROUP BY; cohort is still single-config and stable.
- LLM-confidence battery is N/A for this window (rule-based `degen`, `use_llm: false`) — see that section.

---

## Per-day breakdown (added 2026-06-16)

| Day (WIB) | Trades | Avg PnL | SL share | Total SOL |
|---|---|---|---|---|
| 2026-06-13 | 25 | **−5.52%** | 56.0% | −0.0690 |
| 2026-06-14 | 44 | +2.07% | 43.2% | +0.0455 |
| 2026-06-15 | 60 | +2.00% | 36.7% | +0.0600 |
| 2026-06-16 | 41 | +2.55% | 48.8% | +0.0522 |

The window is **not uniform.** Jun 13 (−5.52% avg, 56% SL share, −0.069 SOL) is the sole drag; Jun 14–16 are clean positives (+2.0% to +2.6%, all SOL-positive) under identical config. Jun 13 is the partial day where trading resumed after the breaker-latch fix (852e04b) — a thin, selloff-heavy restart, not an entry-config problem. Excluding it, the window runs ~+2.2% avg. This is the same regime-day signature as Jun 07 in the [Jun 07–10 eval](evaluate-3d-20260607-0523_20260610-0523.md): one day sets the headline; config is stable across all four days. Reinforces **hold**.

## Circuit breaker

None — the SL-streak breaker emitted no `decision_logs` rows this window. The synced breaker-observability hooks only landed in code on 2026-06-16 (see below) and are not yet deployed to the production bot, so breaker activity for this window can't be confirmed from analytics. First gradeable on the next 3d window after deploy.

---

## Comparison vs last three 3d evals

| Window | Trades | Win % | Avg PnL | Median | Total SOL | SL share | SL avg | TP avg |
|---|---|---|---|---|---|---|---|---|
| Jun 03–06 | 81 | 53.1% | +3.14% | +1.83% | — ¹ | 38.3% | −12.52% | +13.00% |
| Jun 07–10 | 174 | 45.4% | −1.76% | −10.09% | −0.153 | 50.6% | −19.25% | +16.15% |
| Jun 10–13 | 13 ² | 46.2% | −0.06% | −1.07% | −0.0004 | 46.2% | −16.11% | +13.70% |
| **Jun 13–16** | **157** | **57.3%** | **+1.50%** | **+2.53%** | **+0.118** | **41.4%** | **−18.98%** | **+15.97%** |

¹ Jun 03–06 total SOL was estimated (0.1 SOL × Σpnl%), not real `pnl_sol` — not comparable.
² Jun 10–13 was the breaker-latch window: 13 trades in one session, then trading froze ~2.7 days (see [that eval](evaluate-3d-20260610-0750_20260613-0750.md)). Treat as halted, not a regime read.

**Arc:** healthy baseline (Jun 03–06) → crash window (Jun 07–10, root-caused to the single Jun 07 dump day) → breaker latch (Jun 10–13, fixed Jun 13) → **clean recovery (this window).** This window posts the best win rate of the four and is net-positive on a full 157-trade sample. The two-cluster shape (trailing-TP +13–16% winners vs SL −12.5% to −19.3% rug tail) is invariant across all four — the headline swings are regime + the latch, never entry quality. Every report's recommendation has been **hold**; this window confirms the config is net-positive whenever trading isn't frozen and no crash day lands.

---

## Code changes applied 2026-06-16

These do not change trading behaviour — they make the **circuit breaker observable** in analytics so the next window can grade it (right now breaker firings can only be inferred from candidate-flow gaps, as the Jun 10–13 eval had to do).

### 1. `checkSlCooldown` returns firing detail (`src/pipeline/orchestrator.js`)

```js
// before:
function checkSlCooldown(stratId) {
  const key = `sl_cooldown_until_${stratId}`;
  if (Date.now() < numSetting(key, 0)) return true;
  const threshold = numSetting('sl_streak_cooldown_count', 3);
  const cooldownMs = numSetting('sl_streak_cooldown_ms', 3600000);
  const recent = recentClosedExits(stratId, threshold, Date.now() - cooldownMs);
  if (recent.length >= threshold && recent.every(p => p.exit_reason === 'SL')) {
    setSetting(key, String(Date.now() + cooldownMs));
    return true;
  }
  return false;
}

// after:
function checkSlCooldown(stratId) {
  const key = `sl_cooldown_until_${stratId}`;
  const threshold = numSetting('sl_streak_cooldown_count', 3);
  const cooldownMs = numSetting('sl_streak_cooldown_ms', 3600000);
  const until = numSetting(key, 0);
  if (Date.now() < until) return { active: true, armed: false, until, threshold, cooldownMs };
  const recent = recentClosedExits(stratId, threshold, Date.now() - cooldownMs);
  if (recent.length >= threshold && recent.every(p => p.exit_reason === 'SL')) {
    const newUntil = Date.now() + cooldownMs;
    setSetting(key, String(newUntil));
    return { active: true, armed: true, until: newUntil, threshold, cooldownMs };
  }
  return null;
}
```

### 2. Breaker skip emits a synced `decision_logs` row (`src/pipeline/orchestrator.js`)

```js
// before:
if (checkSlCooldown(strat.id)) {
  agentLog.info(`sl streak cooldown active (${strat.id}), skipping ${signals.mint.slice(0, 8)}...`);
  return;
}

// after:
const cooldown = checkSlCooldown(strat.id);
if (cooldown) {
  agentLog.info(`sl streak cooldown active (${strat.id}${cooldown.armed ? ', just armed' : ''}), skipping ${signals.mint.slice(0, 8)}...`);
  logDecisionEvent({
    decision: { selected_mint: signals.mint },
    action: cooldown.armed ? 'entry_skipped_sl_cooldown_armed' : 'entry_skipped_sl_cooldown',
    strategyId: strat.id,
    guardrails: {
      cooldownUntilMs: cooldown.until,
      slStreakThreshold: cooldown.threshold,
      cooldownMs: cooldown.cooldownMs,
    },
  });
  return;
}
```

`entry_skipped_sl_cooldown_armed` = distinct firings (the candidate that tripped the streak); `entry_skipped_sl_cooldown` = each subsequent suppressed candidate. `decision_logs` is already in the synced `TABLES` registry, so these reach Postgres with no schema change.

### 3. `logDecisionEvent` accepts an explicit `strategyId` (`src/db/decisions.js`)

Needed because the breaker fires *before* a candidate is built, so `strategy_id` can't be derived from a candidate snapshot.

```js
// before:
  action,
  guardrails = {},
  execution = {},
}) {
  const selectedCandidate = selectedRow?.candidate || null;
  const strategyId = selectedCandidate?.filters?.strategy
    || rows.find(row => row?.candidate?.filters?.strategy)?.candidate?.filters?.strategy
    || null;

// after:
  action,
  guardrails = {},
  execution = {},
  strategyId = null,
}) {
  const selectedCandidate = selectedRow?.candidate || null;
  const resolvedStrategyId = strategyId
    || selectedCandidate?.filters?.strategy
    || rows.find(row => row?.candidate?.filters?.strategy)?.candidate?.filters?.strategy
    || null;
```

(The `INSERT` now binds `resolvedStrategyId` instead of `strategyId`.)

### 4. `pg-sql.js` sources `.env` (`scripts/pg-sql.js`)

```js
// before:
import pg from 'pg';

const { Pool } = pg;

// after:
import pg from 'pg';
import dotenv from 'dotenv';

dotenv.config();

const { Pool } = pg;
```

The metric battery now runs standalone — no hand-injected `POSTGRES_URL` (resolves the Notes caveat above).

### 5. Evaluator battery extended (`.claude/agents/charon-evaluator.md`)

Added two standing queries so future windows surface this analysis by default instead of ad-hoc:
- **Step 6 — per-day breakdown** (the regime lens; the table at the top of this section is its first output).
- **Step 7 — circuit-breaker activity** (reads the new `decision_logs` rows: firings vs suppressed entries).

Both feed new **## Per-day breakdown** and **## Circuit breaker** report sections and `Per-day:` / `Breaker:` summary lines.

**Verification:** `npm run check` plus `node --check` on both edited source files pass. Steps 6–7 queries were run live against Postgres (step 6 produced the table above; step 7 returned empty, as expected pre-deploy). The breaker skip-logging path itself is **not** verifiable headlessly — it requires 3 consecutive live SLs; first real confirmation is the next crash day post-deploy.
