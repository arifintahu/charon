---
window: 1d
generated_at: 2026-05-14 22:20 +0700
range_start: 2026-05-13 22:20 +0700
range_end: 2026-05-14 22:20 +0700
closed_trades: 154
win_rate_pct: 14.9
avg_pnl_pct: -16.51
total_pnl_sol: -1.27
data_source: remote Postgres (pooled across instances)
---

# Charon Evaluation — 1d

**Window:** 1d · **Range:** 2026-05-13 22:20 → 2026-05-14 22:20 (+0700)
**Generated:** 2026-05-14 22:20 +0700 · **Data source:** remote Postgres — pooled across bot instances; this machine's local SQLite is empty for the window.

## Headline

| Metric | Value |
|---|---|
| Closed trades | 154 |
| Win rate | 14.9% |
| Avg PnL | -16.51% |
| Median PnL | -31.96% |
| Total PnL | -1.27 SOL |

## By strategy

| Strategy | Trades | Wins | Win rate | Avg PnL |
|---|---|---|---|---|
| degen | 154 | 23 | 14.9% | -16.51% |

`degen` was the only active strategy in the window.

## By exit reason

| Exit reason | Trades | Share | Avg PnL | Avg hold |
|---|---|---|---|---|
| SL | 131 | 85% | -35.78% | 32.0 min |
| TRAILING_TP | 23 | 15% | +93.26% | 84.5 min |

No flat `TP` exits — winners only closed via the trailing stop.

## By LLM confidence

| Bucket | Trades | Win rate | Avg PnL |
|---|---|---|---|
| 80+ | 2 | 0.0% | -37.57% |
| 70-79 | 127 | 16.5% | -14.49% |
| 60-69 | 25 | 8.0% | -25.04% |

No trades in the 50-59 bucket. Buckets are non-monotonic — the 80+ cohort is the worst.

## Config drift

Stable. All 153 enriched rows share one config: `tp=30, sl=-15, trail=10, min_mcap=5000, max_mcap=100000`. `TRENDING_MIN_SWAPS` / `TRENDING_MIN_VOLUME_USD` are null in snapshots. 1 pre-snapshot row (null tp/sl) excluded. Cohort is apples-to-apples.

## Patterns

- SL dominates exits: 131/154 (85%) at -35.78% avg — the -15% stop is being blown straight through to -35%, so entries are catching tokens mid-dump.
- Low-hit fat-tail profile: 14.9% win rate, but `TRAILING_TP` winners average +93.26%. Still net -1.27 SOL because 131 losers swamp 23 winners.
- LLM confidence has near-zero discriminative power — the 80+ bucket (n=2) underperforms the 70-79 bucket.

## Recommendations

Listed in priority order.

1. **Tighten entry quality** — SL fires on 85% of trades (131/154) at -35.78% avg, blowing through the -15% stop. Raise `min_mcap_usd` above 5000, or set the currently-unset `TRENDING_MIN_SWAPS` / `TRENDING_MIN_VOLUME_USD` floors. Widening the stop would only deepen losers.
2. **Validate or disable `degen`** — it runs `enabled: true` with no `validation` block (boot warning). Run `npm run backtest -- --strategy degen --from 7d --validate-strategy`. At -16.51% avg PnL it may fail the `avg PnL > 0` criterion, which itself argues against keeping it live.
3. **Capture more of the winners** — to break even at a 14.9% win rate, winners must average ~+215% the loser magnitude; they sit at +93% (2.6x). Consider widening `trailing_percent` from 10 so the winning cohort runs further.
4. **Drop `llm_min_confidence` as a lever** — buckets are non-monotonic (80+ is worst). Raising the threshold would mostly cut the best bucket (70-79). Treat the LLM gate as non-load-bearing for `degen` this window.

## Config changes applied

Applied to `strategies/degen.json` on 2026-05-14 22:49 +0700 in response to this evaluation. Config-only — no source-code changes (the SL overshoot is polling latency between 10s position polls, not a bug; the filter fields were already wired up in `candidateBuilder.js`).

| Field | Initial | Updated | Status |
|---|---|---|---|
| `min_mcap_usd` | 5000 | 15000 | changed |
| `trending_min_volume_usd` | 0 | 3000 | changed |
| `trending_min_swaps` | 0 | 50 | changed |
| `trailing_percent` | 10 | 15 | changed |
| `llm_min_confidence` | 0 | 70 | changed |
| `enabled` | true | true | unchanged |

Not yet live: the bot hot-reads strategy config from SQLite — restart or run `node scripts/cmd.js resetstrategies confirm` to apply. `degen` still has no `validation` block; run `npm run backtest -- --strategy degen --from 7d --validate-strategy` to populate it.

## Notes

- `/learn 1d` was a no-op: local SQLite is empty on this machine; all 154 trades are pooled in remote Postgres from another bot instance. Every metric above comes from Postgres.
