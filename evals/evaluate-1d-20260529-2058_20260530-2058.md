---
window: 1d
generated_at: 2026-05-30 20:58 +0700
range_start: 2026-05-29 20:58 +0700
range_end: 2026-05-30 20:58 +0700
closed_trades: 97
win_rate_pct: 28.9
avg_pnl_pct: -2.21
total_pnl_sol: -0.1074
data_source: remote Postgres (pooled across instances)
---

# Charon Evaluation — 1d

**Window:** 1d · **Range:** 2026-05-29 20:58 +0700 → 2026-05-30 20:58 +0700
**Generated:** 2026-05-30 20:58 +0700 · **Data source:** remote Postgres (pooled across instances)

## Headline

| Metric | Value |
|---|---|
| Closed trades | 97 |
| Win rate | 28.9% |
| Avg PnL | -2.21% |
| Median PnL | -10.64% |
| Total PnL | -0.1074 SOL |

## By strategy

| Strategy | Trades | Wins | Win rate | Avg PnL |
|---|---|---|---|---|
| degen | 97 | 28 | 28.9% | -2.21% |

## By exit reason

| Exit reason | Trades | Share | Avg PnL | Avg hold |
|---|---|---|---|---|
| SL | 54 | 55.7% | -19.90% | 2.5 min |
| TRAILING_TP | 43 | 44.3% | +19.99% | 10.3 min |

## By LLM confidence

LLM not used (rule-based strategy) — confidence battery N/A. `degen` runs `use_llm: false`; every closed trade carries the orchestrator's hardcoded `confidence: 100` rule-based sentinel, which collapses the bucket table to a single `80+` row (n=97). This is by design, not a screener bug.

## Config drift

Stable — all 97 trades share one config row: tp=30, sl=-10, trail=18, min_mcap=50000, max_mcap=100000, trending_min_swaps=100, trending_min_volume_usd=5000, use_llm=false. No mid-window change; cohort is apples-to-apples.
No pre-snapshot / null-tp-sl cohort present (0 of 97 rows had null `$.strategy.tp_percent`).

## Patterns

- Clean binary outcome: 55.7% of trades hit SL at -19.90% avg (2.5 min hold), 44.3% hit TRAILING_TP at +19.99% (10.3 min hold). Losers die fast, winners run ~4x longer — the trailing stop is doing its job of letting winners breathe.
- Win/loss magnitudes are near-symmetric (+19.99% vs -19.90%), so at 28.9% win rate the strategy is slightly net-negative this window (-2.21% avg, -0.1074 SOL). Edge is thin but the loss is small.
- SL fills split into two bands: 48 of 54 are near-stop (-30..-10, avg -14.22%, configured stop is -10%) and only 6 are deep (<=-30, avg -65.34%, worst -98.33%). The 6 deep fills are the known rug / instant-dump tail — slippage past the -10% stop on tokens that collapse before the poll can exit.

## Recommendations

1. **Hold `degen` config as-is** — the strategy is roughly break-even (-2.21% avg, -0.1074 SOL on 97 trades) with near-symmetric win/loss magnitudes and a working trailing stop. No single lever is mispriced; the negative is driven by win rate (28.9%), not by exit placement. No change recommended on this window's evidence.
2. **Do not re-tune to chase the deep-SL tail** — the 6 deep fills (avg -65.34%, worst -98.33%, ~6% of trades) are the rug / instant-dump tail. Entry filters, mcap raises, poll-cadence and stop-placement mitigations have all been investigated from both entry and exit sides and are dead ends. Treat it as a priced-in cost of the strategy.

## Notes

- Rolling-window timing artifact: the headline battery captured 97 closed trades; follow-up queries run seconds later saw 98 (NOW() advanced and one more trade closed inside the live window). SL(54) + TRAILING_TP(43) = 97 is internally consistent for the reported snapshot. No null `pnl_percent` or `exit_reason` rows in the cohort.
- LLM confidence buckets are N/A by design (`use_llm: false`); no `llm_min_confidence` gate or `src/pipeline/llm.js` fix is applicable.
- `/learn` not run — metrics-only evaluation, no narrative lessons requested.
- `scripts/pg-sql.js` reads `process.env.POSTGRES_URL` and does not load `.env`; queries were run with `node --env-file=.env` so the pooled remote Postgres was reachable.

---

## Follow-up analysis (2026-05-30, post-eval deep-dive)

Added after the metric battery in response to a cross-window comparison + a trailing-stop backtest request. The single-window verdict above (hold) stands; this section records why and one deferred lever.

### Trend vs prior windows

| Window | Win% | Avg PnL% | Net SOL | SL avg | TP avg | R/R |
|---|---|---|---|---|---|---|
| 05-24→25 | 32.1 | -2.12 | -0.16 | -14.82 | +14.30 | ~1:1 |
| 05-25→26 | 37.0 | +4.21 | +0.23 | -20.01 | +34.49 | ~1.7:1 |
| 05-26→29 (3d) | 31.4 | -2.01 | -0.31 | -18.51 | +22.60 | ~1.2:1 |
| **05-29→30 (this)** | **28.9** | **-2.21** | **-0.11** | -19.90 | +19.99 | **~1:1** |

- The 05-25 +4.21% day did not hold — this window (-2.21%) matches the 3d window (-2.01%); the green day reads as variance.
- Reward/risk has compressed across the run: as filters tightened (min_mcap 5k→50k) and SL fills improved (-35.8%→-19.9%), the big trailing winners collapsed too (+93%/+107% in mid-May → +19.99% now). Breakeven win rate rose ~28%→~50% faster than win rate rose (15%→~30%), so the strategy is structurally stuck slightly negative. This window's +19.99%/-19.90% is dead-symmetric 1:1 — needs ~50% wins, delivers ~29%.

### Trailing-stop 15 vs 18 (live, since trail→18 shipped 05-29)

Fair adjacent comparison — actual closed degen positions grouped by the `trailing_percent` they ran with, trail=15 sliced to the 7 days immediately before the switch (same `use_llm=false` era):

| trailing_percent | Window | Closed | Win% | Avg PnL% | Total SOL | Trailing exits (avg) | SL exits (avg) |
|---|---|---|---|---|---|---|---|
| 15 | 05-23→05-29 (7d) | 806 | 30.9 | -1.43 | -0.577 | 318 (+22.64%) | 488 (-17.12%) |
| 18 | 05-29→05-30 (~1d) | 106 | 29.2 | -2.33 | -0.123 | 47 (+19.50%) | 59 (-19.71%) |

- No signal: trail=18's first day is marginally worse on every headline (win, avg PnL, trailing capture +19.50% vs +22.64%), all within one-day noise (n=106, 47 trailing exits vs an 806-trade baseline).
- Trailing capture trending *below* baseline is the opposite of the "looser trail lets winners run further" thesis — most likely the market-condition confound (recent runners are smaller; the +90–110% trailing days were early May). Live data can't separate the parameter from the market.
- Re-read ~06-06, once trail=18 accumulates ~300 trailing exits (parity with the trail=15 baseline). Metric to watch: trailing-exit avg %. Query spec saved at `evals/sweep-trailing-percent.json`.

### Backtester can't arbitrate this (broken)

A `trailing_percent` sweep [12,15,18,21] over 7d returned an identical flat -10% SL wall for every cell, because validation fails hard: 27.6% within ±5% PnL (gate ≥80%), 77.2% exit_reason match (gate ≥90%), and **0 of 328 actual TRAILING_TP exits reproduced**. Root cause: the simulator's candle prices are ~83x lower than the stored `entry_price`/`metrics.priceUsd` (range 63–117x — a units/decimals mismatch in candle ingestion, `src/backtest/candles.js` vs `src/backtest/runner.js:144`), so SL fires on candle #1 and the trailing never arms. Until fixed, the backtester can't replay trail settings, validate degen, or produce the `validation` block degen needs to legitimately carry `enabled: true` (it currently runs `enabled: true` with `validation: null`). Fix deferred 2026-05-30.

### Entry-mcap gradient (the one data-backed entry lever)

Win rate / PnL by entry-mcap bucket (use_llm=false era, n≈1500):

| Entry mcap | n | Win% | Avg PnL% | Total SOL | Trailing avg |
|---|---|---|---|---|---|
| 30–40k | 22 | 13.6 | -23.45 | -0.258 | +40.9% |
| 40–50k | 250 | 28.8 | -2.43 | -0.304 | +31.3% |
| 50–60k | 348 | 31.9 | -1.62 | -0.282 | +24.4% |
| 60–70k | 285 | 29.1 | -1.29 | -0.184 | +24.7% |
| 70–80k | 229 | 29.3 | **+0.41** | **+0.047** | +26.7% |
| 80–90k | 192 | 33.3 | -0.67 | -0.065 | +23.0% |
| 90–100k | 161 | 28.6 | **-4.53** | **-0.365** | **+16.6%** |

- 30–50k (below the live 50k floor) are net-negative — validates the min_mcap ladder.
- The 90–100k bucket is the worst in-band cohort: -4.53% avg, -0.365 SOL, and the lowest trailing capture (+16.6% vs +24–27% below) — tokens near the 100k top have the least room to run.
- **Deferred lever:** lower `max_mcap_usd` 100000→90000. Cuts the 90–100k drag, lifts the sample book -1.40%→-0.92% avg, costs ~13% of entries. A tighter 60–90k band reaches ~-0.57% but drops ~42% of entries (two changes — worse for attribution). Can't backtest first (above); apply directly when revisited.
- Partial TP is *not* a lever here — it's a no-op in `dry_run` (`src/execution/positions.js:160` runs the partial sell live-only), so it can't be paper-tested and wouldn't show in this data.

### Decision (2026-05-30): hold

No config change applied. It's paper money (`dry_run`) with no urgency, and stacking a change now would muddy attribution on the noisy daily signal — particularly the trail=18 read that needs ~a week to mature. Revisit ~06-06: re-run this window + the trailing split, then apply `max_mcap→90k` or shelve degen if it stays ~-1%.
