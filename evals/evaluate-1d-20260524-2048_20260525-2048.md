---
window: 1d
generated_at: 2026-05-25 20:48 +0700
range_start: 2026-05-24 20:48 +0700
range_end: 2026-05-25 20:48 +0700
closed_trades: 156
win_rate_pct: 32.1
avg_pnl_pct: -2.12
total_pnl_sol: -0.1613
data_source: remote Postgres (pooled across instances)
---

# Charon Evaluation — 1d

**Window:** 1d · **Range:** 2026-05-24 20:48 +0700 → 2026-05-25 20:48 +0700
**Generated:** 2026-05-25 20:48 +0700 · **Data source:** remote Postgres (pooled across instances)

## Headline

| Metric | Value |
|---|---|
| Closed trades | 156 |
| Win rate | 32.1% |
| Avg PnL | -2.12% |
| Median PnL | -10.38% |
| Total PnL | -0.1613 SOL |

## By strategy

| Strategy | Trades | Wins | Win rate | Avg PnL |
|---|---|---|---|---|
| degen | 156 | 50 | 32.1% | -2.12% |

## By exit reason

| Exit reason | Trades | Share | Avg PnL | Avg hold |
|---|---|---|---|---|
| SL | 88 | 56.4% | -14.82% | 2.2 min |
| TRAILING_TP | 68 | 43.6% | +14.30% | 5.9 min |

## By LLM confidence

| Bucket | Trades | Win rate | Avg PnL |
|---|---|---|---|
| 80+ | 156 | 32.1% | -2.12% |

All 156 decisions fall in the 80+ confidence bucket — no spread to analyze. Confidence is not a usable discriminator for this cohort.

## Config drift

Strategy `degen` ran two `sl_percent` values mid-window — cohort is not apples-to-apples:
- `sl=-10`: 151 trades, avg -2.56% (85 SL exits, 66 TRAILING_TP) — the dominant, currently-active config.
- `sl=-8`: 4-5 trades, avg +11 to +16% (2 SL, 2 TP) — the config reverted by commit d19f0cf; near-zero sample, not conclusive.

All other gated fields are stable across both cohorts: tp=30, trail=15, min_mcap=40000, max_mcap=100000.
No pre-snapshot / null-tp-sl cohort — all 155 snapshot-bearing rows carry full strategy config; `TRENDING_MIN_SWAPS` / `TRENDING_MIN_VOLUME_USD` are null in the env snapshot (degen is trending-poller driven; those keys are unset).

## Patterns

- **Asymmetric, bimodal PnL.** Losers cluster tightly at the stop: 75 of 156 trades land in the -10% to -20% band (right at the sl=-10 trigger), while winners form a thin long tail (5 trades each at +44–50% and +57–75%). Winners do pay (TRAILING_TP avg +14.30%, best +74.71%) but only 43.6% of trades reach the trail.
- **Stops fire fast and often.** SL is the majority exit (88/156, 56.4%) with a 2.2 min avg hold and 23 trades stopped out in under 60 seconds — premature shake-outs before any trend forms. SL median is -12.92% (overshooting the -10% trigger), indicating gap-through fills on fast-moving degen tokens. No `stop_loss_arm_delay_sec` is set on the dominant cohort.
- **The math doesn't close.** 32.1% win rate × +14.30% avg win = +4.59 expected gain per trade vs 67.9% × -14.82% = -10.06 expected loss — net negative. The win rate is too low for the current reward/risk to break even.

## Recommendations

156 closed trades (≥10) — listed in priority order:

1. **Stop-loss arm delay** — 23/88 SL exits closed in under 1 min and SL avg hold is just 2.2 min, signalling entry-spike shake-outs that stop out before a trend forms. → Set `stop_loss_arm_delay_sec` on `degen` (try 30–60s) so the stop doesn't arm during the first post-entry candle. The `stop_loss_arm_delay` feature already exists (commit 74408a9); the active cohort has it unset.
2. **Tighten entry selectivity to lift win rate** — 32.1% win rate with +14.30%/-14.82% reward/risk is net-negative (avg -2.12%, -0.1613 SOL). → Raise the degen entry bar (e.g. mcap floor above 40000, or stricter trending volume/swap gates) to cut the -10% to -20% loser cluster (75/156 trades) before expanding size.
3. **Re-evaluate the sl=-10 vs sl=-8 revert with a real sample** — the reverted sl=-8 cohort shows avg +11 to +16% but only on 4-5 trades; the active sl=-10 cohort is -2.56% over 151. → Don't act on the 5-trade signal, but run a backtest sweep (`npm run backtest -- --strategy degen --from 7d` with sl overrides) to settle -8 vs -10 on volume before the next config change.
4. **Let winners run further** — TRAILING_TP median is +7.41% but the tail reaches +74.71%; trail=15 may be clipping runners early. → Backtest a wider `trailing_percent` (e.g. 20–25) or a `trailing_arm_threshold_percent` so the trail only tightens after a token has proven momentum.

## Notes

- Headline/per-strategy COUNT returned 156; the drift and completeness queries (run moments later) returned 155 as `NOW()` advanced past one boundary-closing position between calls. Treat 156 as the window total; the 1-row delta is timing, not data loss.
- All 155 snapshot-bearing positions have a non-null LLM decision link and snapshot (`no_llm_link=0`, `no_snapshot=0`) — no pre-snapshot cohort to exclude.
- `/learn 1d` not run — the metric battery already isolated the actionable levers (arm delay, entry selectivity, trail width); narrative lessons would add an LLM call without changing the recommendations.
- `pg-sql.js` does not load `.env`; queries were run via `node --env-file=.env scripts/pg-sql.js` to supply `POSTGRES_URL`. The pg SSL deprecation warning (`sslmode=require` → `verify-full`) is benign and does not affect results.
- Data source is remote Postgres pooled across instances; this window is entirely `degen` strategy, single machine evident from the uniform config.

---

## 13-day trend comparison (May 13 → May 25)

| Date | Trades | Win rate | Avg PnL | Total PnL | SL% | SL avg fill | TRAILING_TP% | TP avg | Key change |
|---|---|---|---|---|---|---|---|---|---|
| May 13–14 | 154 | 14.9% | -16.51% | -1.27 SOL | 85% | -35.78% | 15% | +93.26% | baseline |
| May 14–15 | 137 | 15.3% | -12.94% | -0.89 SOL | 85% | -34.68% | 15% | +107.12% | min_mcap 5k→15k, llm on |
| May 15–16 | 131 | 22.9% | -5.56% | -0.36 SOL | 77% | -36.61% | 23% | +98.97% | min_mcap 15k→30k, sl -15→-10 |
| May 16–17 | 86 | 23.3% | -8.17% | -0.35 SOL | 76% | -33.86% | 24% | +71.32% | rug filters (smart_degen, hot_level, top_holder) |
| May 17–18 | 104 | 16.3% | -9.76% | -0.51 SOL | 83% | -27.12% | 17% | +73.21% | liq floor, mcap/liq ratio, LLM prompt rewrite |
| **May 18–19** | 162 | **34.0%** | -1.86% | — | **60%** | -19.30% | **40%** | +24.16% | LLM off, GMGN rug filters, trailing_arm=15, min_mcap→40k |
| **May 19–20** | 108 | **30.6%** | **-0.68%** | **-0.04 SOL** | 60% | -16.40% | 40% | +23.07% | hour blackout [20,21,22], liq drain gate, bot_degen→0.50 |
| May 20–21 | 97 | 24.7% | -3.12% | -0.15 SOL | 66% | -15.40% | 34% | +20.69% | sl_arm_delay=30s (later reverted) |
| *(gap May 21–23)* | — | — | — | — | — | — | — | — | sl_arm_delay→0, sl -10→-8, arm 15→12 |
| May 23–24 | 147 | 21.8% | -6.17% | -0.45 SOL | 71% | -16.54% | 29% | +18.90% | regression — sl→-8, arm→12 |
| **May 24–25** | **156** | **32.1%** | **-2.12%** | **-0.16 SOL** | **56%** | **-14.82%** | **44%** | +14.30% | revert d19f0cf (sl→-10, arm→15) |

### Key observations

- **The revert (commit d19f0cf) is vindicated.** Every "what to watch" target from the May 23–24 eval was met or beaten: win rate 21.8% → 32.1% (predicted 28–31%), SL share 71% → 56.4% (**lowest in the dataset**), SL fill -16.54% → -14.82% (**best in the dataset**), TRAILING_TP share 29% → 43.6%. The only miss is TP avg, which came in *lower* (+14.30% vs the predicted +20–23%).
- **Structural shift — winners keep compressing.** TP avg has fallen +93% → +24% → **+14.30%** as exits tightened, while SL fill settled ~-15%. Reward/risk has compressed from ~2.6:1 (early) to **~1:1**, so the breakeven win rate has *climbed* from ~28% to **~51%** (`14.82 / (14.82 + 14.30)`). Today is 3rd-best on avg PnL despite a near-peak win rate because the absolute swings shrank: the book bleeds slower but 32% win rate buys less than it did at the May 18–19 peak. The strategy is converging on "many small wins, many small losses, slightly net-negative."
- **Position in trajectory:** back into the May 18–20 peak cluster — 2nd-best win rate (32.1% vs peak 34.0%), 3rd-best avg PnL and total SOL.
- **⚠️ Rec #1 contradicts a prior validated finding.** This eval's top recommendation (set `stop_loss_arm_delay` ~30–60s) is the exact change shipped May 20 (`sl_arm_delay_ms: 30000`) and **reverted May 21** — the May 20–21 eval found it *extended losing holds 2.6 → 4.6 min and dropped win rate ~6pp*. It reads as unset (true since the revert) but does not flag that it already backfired. **Not re-applied** in this session's change below.

---

## Config changes applied — 2026-05-25

### Diagnosis

The binding constraint is no longer loss magnitude — the GMGN rug filters and the sl=-10 revert already pulled SL fill down to -14.82% (best in the dataset). It is **win rate**: 32.1% against a breakeven that the reward/risk compression has pushed to ~51%. Lifting win rate requires entry selectivity. Of the four eval recommendations:

| Rec | Action | Verdict |
|---|---|---|
| #1 stop-loss arm delay | re-add `sl_arm_delay` ~30–60s | **Skip** — shipped May 20, reverted May 21 as a regression (see trend flag) |
| #2 tighten entry selectivity | raise mcap floor / trending gates | **Ship this** — directly lifts win rate, backtested support exists |
| #3 settle sl=-8 vs -10 | backtest sweep | Not a live change; user declined a fresh backtest this session |
| #4 wider `trailing_percent` | 20–25 to stop clipping the tail | **Skip** — direction is ambiguous (median trail exit is only +7.41%, so tighter could equally apply) and trail params have whipsawed (May 18→21→24). Needs a backtest, not a blind ship |

### Change

`min_mcap_usd` raised 40000 → 50000 on `degen` (config-only — no source changes).

**Evidence:** the 2026-05-18 win-rate uplift investigation (7d / 667 closed trades) found `mcap ≥ 50000` was the **single best entry filter** — 24.8% win rate, best precision/recall, retains 52% of winners while blocking 64% of SL trades. It was passed over then in favor of the "volume-preserving" 40k floor; with the strategy net-negative and win rate the constraint, the quality floor is now the right trade.

`strategies/degen.json`:

| Field | Before | After | Reason |
|---|---|---|---|
| `min_mcap_usd` | `40000` | `50000` | Single best entry filter on the 7d cohort: 24.8% win rate, 52% winner retention, blocks 64% of SL |

All other degen fields unchanged. No code changes. No other strategy files touched.

### Verification

- `node scripts/cmd.js resetstrategies confirm` applied cleanly (degen still warns "enabled without a validation block" — pre-existing, unrelated).
- SQLite working copy confirmed: `json_extract(config_json,'$.min_mcap_usd') = 50000` on the degen row (`sl_percent=-10`, `trailing_arm_percent=15` unchanged).

### Apply

```
node scripts/cmd.js resetstrategies confirm    # already run; bot hot-reads within 5s
# or: npm start
```

### What to watch (next 1d window)

- **Trade count drops** — the 40k–50k mcap band is removed. Expect a meaningful cut (possibly 30–50%). If it falls **>50%**, 50k is too aggressive; step back to 45k.
- **Win rate lifts** — target above the mid-30s. If it stays ~32% on materially lower volume, the mcap band wasn't the discriminator and the next lever is liquidity floor (16k→18k, the May 18 #2 filter) or a backtested trail change.
- **SL share** — should hold ≤56% or improve; higher-mcap tokens gap through stops less.
- **Not changed, deliberately:** trail width/arm (ambiguous, whipsaw-prone — backtest before touching) and stop-loss arm delay (reverted May 21).
